#include <iostream>
using namespace std;
#include<string>

int main() {
    string str;
    cin >> str;
    if(str[0] == 'F') cout <<"Foundation";
    if(str[0] == 'C') cout <<"Claves";
    if(str[0] == 'V') cout <<"Veritas";
    if(str[0] == 'E') cout <<"Exploration";
    
    
}